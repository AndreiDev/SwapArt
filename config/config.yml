defaults: &defaults
  genders:
    - female
    - male
    - 'other or decline to state'
  default_gender: 'other or decline to state'
  omniauth_path_prefix: "/auth"
  #kerberos_callback_path: "/users/auth/kerberos/callback"
  site_domain: "http://localhost:3000/"
  site_name: "SwapArt"
  author: "Andrei Averkin"
  meta_description: "Swap art with people like you"
  meta_keywords: "swap, art, free, network"

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
