<%= simple_form_for(@tagging) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :item, label_method: :description, value_method: :id %>
    <%= f.association :tag, label_method: :description, value_method: :id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
