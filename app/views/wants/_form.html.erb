<%= simple_form_for(@want) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user, label_method: :full_name, value_method: :id %>
    <%= f.association :item, label_method: :description, value_method: :id %>
    <%= f.input :extra_mile? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
