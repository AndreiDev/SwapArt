<%= simple_form_for(@want) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :item %>
    <%= f.input :extra_mile? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
