<%= simple_form_for(@item, validate: true, :html => { :class => 'form-inline' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <% if current_user.has_role? :admin %>
      <%= f.association :user, label_method: :full_name, value_method: :id %>
      <%= f.input :is_active %>
      <%= f.input :is_blocked %>
    <% end %>
    <%= f.label :image%>
    <%= f.file_field :image %>
    <br/>
    <%= f.input :description %> <br/>
    <%= f.association :price, label_method: :description, value_method: :id, selected: 11 %><br/>
    <%= f.input :height %>
    <%= f.input :width %>
    <%= f.association :type, label_method: :description, value_method: :id, selected: 11  %>
    <%= f.association :age, label_method: :description, value_method: :id, selected: 11 %>
    <%= f.association :state, label_method: :description, value_method: :id, selected: 11 %>
  </div>

  <div class="form-actions">
    <br/>
    <%= f.button :submit %>
  </div>
<% end %>
